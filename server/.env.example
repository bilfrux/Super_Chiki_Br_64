# Copy to .env (gitignored). Loaded by `npm start`.
ADMIN_KEY=dev-admin-key
PORT=8080
# Only if phones must use a different address than this machine (tunnel / deployment):
# PUBLIC_URL=https://your-host.example.com
